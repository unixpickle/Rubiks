#include "parity.h"

// returns 0 for even parity, 1 for odd parity
int cube_edge_swap_parity(const char * edges) {
    
}

int cube_corner_swap_parity(const char * corners) {
    
}

// validations return 1 if valid or 0 if not
int validate_corner_orientation(const char * corners) {
    
}

int validate_edge_orientation(const char * edges) {
    
}
